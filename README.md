Install:

    npm install phantomjs-prebuilt

Add to crontab:

    PATH=/usr/bin:/bin:/usr/local/bin
    */2 * * * * cd $HOME/m/voting-delta && ./getpictures.sh

Run some of these:

    ./mkcrop.sh
    ./mkmontage.sh
    ./mkmp4.sh

